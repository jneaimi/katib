# Test inputs for `news-cover` isolated render.
# Used by `katib component test news-cover`. Add realistic sample values.
inputs:
  title: "Sample title"
brand: null        # or a registered brand name, e.g. acme
variant: null      # or a declared variant name
