# Test inputs for `data-table` isolated render.
# Used by `katib component test data-table`. Add realistic sample values.
inputs:
  title: "Sample title"
brand: null        # or a registered brand name, e.g. acme
variant: null      # or a declared variant name
