# Test inputs for `sections-grid` isolated render.
# Used by `katib component test sections-grid`. Add realistic sample values.
inputs:
  title: "Sample title"
brand: null        # or a registered brand name, e.g. acme
variant: null      # or a declared variant name
